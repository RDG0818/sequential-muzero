# Overrides for pure-JAX GPU training (train_jax.py).
# All other train params inherit from default.yaml.
num_envs: 32
batch_size: 512
warmup_episodes: 200
num_actors: 0
num_reanalyze_actors: 0
checkpoint_dir: checkpoints_jax
